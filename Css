/* Fonte e corpo */
body {
    font-family: 'Montserrat', sans-serif;
    font-size: 16px;
    transition: all 0.3s ease;
}

/* Logo */
.logo {
    font-family: 'Lemon', cursive;
    font-size: 28px;
}

/* Botão de acessibilidade dentro do header */
.menu-acessibilidade {
    position: absolute;
    top: 10px;
    right: 10px;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    z-index: 1000;
}

.menu-acessibilidade button {
    margin-bottom: 5px;
}

/* Opções da acessibilidade com animação */
.opcoes-acessibilidade {
    background: #f8f9fa;
    border: 1px solid #ccc;
    padding: 8px;
    border-radius: 8px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.2);
    display: flex;
    flex-direction: column;
    opacity: 0;
    transform: translateY(-10px);
    transition: all 0.3s ease;
    pointer-events: none;
}

.opcoes-acessibilidade.show {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
}

/* Contraste */
.contraste {
    background-color: #000 !important;
    color: #ff0 !important;
}

.contraste a {
    color: #ff0 !important;
}

/* Footer */
footer {
    font-size: 14px;
}

footer a {
    color: inherit;
    text-decoration: none;
}

footer a:hover {
    text-decoration: underline;
}

/* Ajustes responsivos */
@media (max-width: 768px) {
    nav ul.nav {
        margin-top: 10px;
        width: 100%;
        justify-content: center;
    }
}
